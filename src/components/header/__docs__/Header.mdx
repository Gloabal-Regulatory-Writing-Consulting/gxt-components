import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as Header from "./Header.stories.tsx";

<Meta of={Header} title="Header" />

# Header

Header component

#### Example

<Canvas>
  <Header/>
</Canvas>

## Usage

```ts
import { Header } from '@gloabal-regulatory-writing-consulting/gxt-components';

const breadcrumbItems = [
  { label: 'Home', to: '/' },
  { label: 'Upload Documents', to: '/upload-documents', active: true },
];

const isLinkActive = (path: string) => {
  // set active string logic
}

const ExampleHeader = () => (
  <Header breadcrumbItems={breadcrumbItems} isLinkActive={isLinkActive}>
    <Header.Heading>Upload Documents</Header.Heading>
    <Header.Actions>
      <Button variant="secondary">Add to Catalog</Button>
      <Button variant="secondary">Cancel</Button>
    </Header.Actions>
  </Header>
);
export default ExampleHeader;
```

## Props
<Controls of={Header.Default} />
