import { Meta, Canvas, Controls } from "@storybook/blocks";
import React from "react";
import PaginationExample from "./PaginationExample"
import * as Pagination from "./Pagination.stories";


<Meta of={Pagination} title="Pagination" />

#### Example

<Canvas of={Pagination.Pagination} />

# Pagination

This is an example component that showcases the usage of `Pagination` component.

## Usage

```tsx
import {Pagination} from "sld-ui";

const Example = () => {
  const [currentPage, setCurrentPage] = useState(0);
  const [perPage, setPerPage] = useState(10);

  const handlePerPageChange = (value: number) => {
    setPerPage(value);
    setCurrentPage(0);
  };

  return (
      <Pagination
        currentPage={currentPage}
        onPageChange={(value) => setCurrentPage(value)}
        itemsPerPage={perPage}
        totalItems={totalItems}
        label={label}
        perPageOptions={perPageOptions}
        handlePerPageChange={handlePerPageChange}
      />
  );
};

export default Example;
```

## Props
<Controls of={Pagination.Pagination} />
